# Intent-classifier-model
About A Tiny Intent Classifier model for short customer-support style text. Given an input text (e.g., "Hi, I need help with my bill"), the model returns one of: `greeting` - `question` - `complaint` - `praise` - `other`.
